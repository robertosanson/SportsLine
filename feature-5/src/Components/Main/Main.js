import MainList from "./MainList";

const MainModule = () => {
  return (
    <div>
      <MainList />
    </div>
  );
};

export default MainModule;

// This is the main page were we are callin ghe MainList
// which contains all of the data for the items and vendors
