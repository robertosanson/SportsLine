// import {logoutUser} from "./AuthService"

// import { useNavigate } from "react-router-dom";

export default function Logout(){
  // const navigate = useNavigate();

  // logoutUser(currentUser).then((userLoggedOut) => {
  //   if (userLoggedOut) {
  //     alert(
  //       `${userLoggedOut.get("firstName")}, you successfully logged out!`
  //     );
  //     navigate("/logOut");
  //   }
  //   // TODO: redirect user to main app
  //   // setAdd(false);
  // });
  
  
  return (
      <section>
            <h2> Logged Out </h2>
      </section>
);}
      
// This is a logout function which we haven't yet finished but we are still working on it.